csi-driver-nfs:
  driver:
    mountPermissions: "0755"

storageClasses:   
  - name: nfs-bind
    mountOptions:
      - nosuid
      - hard
      - nfsvers=4.1
    reclaimPolicy: Retain
    volumeBindingMode: Immediate
  