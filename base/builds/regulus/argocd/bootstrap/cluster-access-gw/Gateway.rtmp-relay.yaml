apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: rtmp-relay
  namespace: default
spec:
  #addresses:
  #- type: IPAddress
  #  value: 192.168.64.249
  gatewayClassName: rtmp-relay
  listeners:
  - allowedRoutes:
      namespaces:
        from: All
    name: http
    protocol: HTTP
    port: 1935
