apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: cluster-ingress
spec:
  addresses:
  - type: "IPAddress"
    value: "192.168.64.244"
  gatewayClassName: cluster-gateway
  listeners:
  - allowedRoutes:
      namespaces:
        from: All
    name: https
    protocol: HTTPS
    port: 9080