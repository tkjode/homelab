apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: cluster-ingress
spec:
  addresses:
  - type: "IPAddress"
    value: "192.168.64.248"
  gatewayClassName: cluster-gateway
  listeners:
  - allowedRoutes:
      namespaces:
        from: All
    name: http
    protocol: HTTP
    port: 9080
