apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarranime-config
  namespace: sonarranime
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: iscsi
  volumeMode: Filesystem
  volumeName: sonarranime-config