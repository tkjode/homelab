apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: inference-projects
resources:
  - AppProject.inference.yaml                       # Creates the `inference` ArgoCD Project
  - manifests/open-webui/Application-open-webui.yaml    # Build started - Open-WebUI Project