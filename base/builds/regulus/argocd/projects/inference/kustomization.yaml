apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: inference-projects
resources:
  - AppProject.inference.yaml                       # Creates the `inference` ArgoCD Project
  - manifests/open-webui/Application-open-webui.yaml    # Build completed
# - manifests/kube-mcp-server/Application.kube-mcp-server.yaml  # This sucks
  - manifests/kubernetes-mcp-server/Application.kubernetes-mcp-server.yaml  # Build Started (Helm)