daemon off;

error_log /dev/stdout info;

events {
  worker_connections 1024;
}

rtmp {
  server {
    listen 1935;
    chunk_size 4096;

    application stream {
      live on;
      record all;
      record_path /nas;
      record_unique on;
      record_suffix -%Y-%m-%d-%H_%M-%S.flv;

      exec ffmpeg -i rtmp://localhost:1935/stream/$name
        -c:a libfdk_aac -b:a 192k -c:v libx264 -b:v 12000k -f flv -g 30 -r 30 -s 1920x1080 -preset fast -profile:v baseline rtmp://localhost:1935/hls/$name_1080p12000k
        -c:a libfdk_aac -b:a 192k -c:v libx264 -b:v 3000k -f flv -g 30 -r 30 -s 640x360 -preset ultrafast -profile:v baseline rtmp://localhost:1935/hls/$name_360p3000k;
    }

    application hls {
      live on;
      hls on;
      hls_fragment_naming system;
      hls_fragment 2s;
      hls_playlist_length 10s;
      hls_path /opt/data/hls;
      hls_continuous on;
      hls_nested on;

      hls_variant _1080p12000k BANDWIDTH=12000000,RESOLUTION=1920x1080;
      hls_variant _360p3000k BANDWIDTH=3000000,RESOLUTION=640x360;
    }
  }
}

http {
  root /www/static;
  sendfile off;
  tcp_nopush on;
  server_tokens off;
  access_log /dev/stdout combined;

  server {
    listen 80;

    location /hls {
      types {
        application/vnd.apple.mpegurl m3u8;
        video/mp2t ts;
      }
      root /opt/data;
      add_header Cache-Control no-cache;
      add_header Access-Control-Allow-Origin *;
    }

    location /live {
      alias /opt/data/hls;
      types {
        application/vnd.apple.mpegurl m3u8;
        video/mp2t ts;
      }
      add_header Cache-Control no-cache;
      add_header Access-Control-Allow-Origin *;
    }

    location /stat {
      rtmp_stat all;
      rtmp_stat_stylesheet stat.xsl;
    }

    location /stat.xsl {
      root /www/static;
    }

    location /crossdomain.xml {
      default_type text/xml;
      expires 24h;
    }
  }
}
