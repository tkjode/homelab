apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: landingpage-projects
resources:
  - AppProject.landingpage.yaml                     # Creates the `landingpage` ArgoCD Project
  - manifests/homepage/Application-homepage.yaml    # Build Started